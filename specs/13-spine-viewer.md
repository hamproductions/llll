# Spine Viewer

A visible placeholder route for a future Spine animation viewer.

## Acceptance Criteria

- A route exists at `/spine-viewer`
- The page clearly communicates that this feature is planned but not yet available
- The existing model viewer at `/model-viewer` is not disrupted or degraded
- No useful existing viewer controls are removed
